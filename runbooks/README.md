# Runbooks

Runbooks contain exact copy, load, and run commands. The versioned originals remain under `assets/runbooks/` and `skills/5t-ota/runbooks/`; the archive preserves copies under `history/runbooks/`.

Use a new empty schematic for first execution. Read the runbook associated with the exact generator filename. Do not infer a procedure name from a filename.
